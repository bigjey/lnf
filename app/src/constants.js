export const TOKEN_STORAGE_KEY = 'auth_token';
export const USER_ID = 'user_id';

export const COLORS = {
  BACKGROUND: '#f3f3f3',
};

export const SERVER='/';

export const api = {
  login: `${SERVER}auth/login`,
  register: `${SERVER}auth/register`,
  post: `${SERVER}api/post`,
}